{
  "name": "serverless-2fa",
  "version": "0.0.1",
  "description": "A serverless two factor authentication framework that uses dynamodb and lambda on aws",
  "main": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Rich Chetwynd, ThisData",
  "license": "MIT",
  "dependencies": {
    "bluebird": "^3.4.6",
    "joi": "^10.0.1",
    "notp": "^2.0.3",
    "thirty-two": "^1.0.2",
    "vogels": "^2.2.0"
  }
}
